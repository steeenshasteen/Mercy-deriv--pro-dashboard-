const profile = { loginId: "", email: "", currency: "", balance: 0, accountType: "" };
function updateProfile(authorizeData) { profile.loginId = authorizeData.loginid || ""; profile.currency = authorizeData.currency || ""; profile.balance = authorizeData.balance || 0; profile.accountType = authorizeData.is_virtual ? "Demo Account" : "Real Account"; renderProfile(); }
function renderProfile() { const profileLogin = document.getElementById("profileLogin"); const profileCurrency = document.getElementById("profileCurrency"); const profileBalance = document.getElementById("profileBalance"); const profileType = document.getElementById("profileType"); if (profileLogin) profileLogin.textContent = profile.loginId; if (profileCurrency) profileCurrency.textContent = profile.currency; if (profileBalance) profileBalance.textContent = Number(profile.balance).toFixed(2); if (profileType) profileType.textContent = profile.accountType; }